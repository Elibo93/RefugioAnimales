server:
  port: 8082
  forward-headers-strategy: native
  servlet:
    encoding:
      charset: UTF-8
      force: true
  error:
    include-message: always
    include-exception: true
    include-stacktrace: always

spring:
  application:
    name: REFUGIO-FRONTEND

  cloud:
    compatibility-verifier:
      enabled: false   # Spring Boot 3.4.4 con Spring Cloud 2024.0.0

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Thymeleaf
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    cache: false

  # i18n — usa los mismos archivos messages copiados del backend
  messages:
    encoding: UTF-8
    basename: messages,common_messages

  # Jackson — soporte para LocalDateTime
  jackson:
    serialization:
      write-dates-as-timestamps: false

# Eureka — el frontend se registra para que el Gateway lo descubra
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

# URL interna del backend (comunicación server-to-server vía Eureka Service Discovery)
backend:
  api:
    url: http://REFUGIO-BACKEND/api

auth:
  api:
    url: http://REFUGIO-AUTH/api


management:
  endpoints:
    web:
      exposure:
        include: health, info

# Claves JWT compartidas para decodificar (Variables de entorno con Fallback local)
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}
